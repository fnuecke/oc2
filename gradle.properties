org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g

enabledPlatforms=neoforge

modId=oc2
mavenGroup=li.cil.oc2

# Set to a sibling checkout to build against it instead of the published artifact.
# Prefer setting these in ~/.gradle/gradle.properties, which takes precedence over this file.
ceresDir=
sednaDir=
buildrootDir=
markdownManualDir=

# Set by build pipeline.
modVersion=0.0.0
